# Pi-hole Configuration Parameters
# Update these to match your local network environment

# The IP address of your router/gateway
ROUTER_IP=192.168.86.1

# Your local network range in CIDR format
LOCAL_NETWORK=192.168.86.0/24

# The IP address of the host machine running Pi-hole
PIHOLE_HOST_IP=192.168.86.32

# Secondary router reverse lookup identifier (last octets in reverse)
# Example: For 192.168.86.x, use 86.168.192.in-addr.arpa
REV_SERVER_DOMAIN=86.168.192.in-addr.arpa

# Admin Password
ADMIN_PASSWORD=admin

# VPN DNS Forwarding (optional)
# Set VPN_DNS_SERVER to forward specific domains to your VPN's DNS
# VPN_DOMAINS is a comma-separated list of domains to forward
# Leave VPN_DNS_SERVER empty to disable VPN forwarding
VPN_DNS_SERVER=
VPN_DOMAINS=attackiq.com,attackiq.net,amazonaws.com
